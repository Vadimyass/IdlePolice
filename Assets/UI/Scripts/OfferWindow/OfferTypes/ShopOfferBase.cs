using System;
using System.Collections.Generic;
using UnityEngine;

namespace Gameplay.Scripts.IAPController
{
    public class ShopOfferBase : MonoBehaviour
    {
        /*[SerializeField] private List<IAPButtonView> _buttons;

        public virtual void Init()
        {
            _buttons.ForEach(x => x.Init());
        }

        public virtual void Dispose()
        {
            _buttons.ForEach(x => x.Dispose());
        }*/
    }
}